# *******************
#  how_to_docker 07 *
# *******************

# 7. Get the internal IP address of the overlord container without starting 
#       its shell and in one command.

docker inspect -f '{{ .NetworkSettings.IPAddress }}' overlord